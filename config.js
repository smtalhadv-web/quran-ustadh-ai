// Public runtime configuration. Safe to leave blank until Supabase is connected.
// Never place a Supabase service_role key in client-side code.
window.QURAN_USTADH_CONFIG = window.QURAN_USTADH_CONFIG || {
  supabase: { url: "", publishableKey: "" }
};